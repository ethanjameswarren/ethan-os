# Planning Domain Examples

## Capture

> i want to actually get good at system design this year. i keep failing those interviews. id know id made it if i could design something like a URL shortener or a rate limiter from scratch without freezing up.

## Resulting objects

- Goal: "Become proficient at system design" (horizon: long_term)
  - why_it_matters: keeps failing system design interviews
  - success_criteria: design common systems (URL shortener, rate limiter) from scratch without freezing up

Since this requires multiple coordinated steps, `breakdown-project` creates:

- Project: "System Design Practice" (goal_id → the goal above)
  - milestones: "Learn core building blocks", "Practice 10 common designs", "Do 3 mock interviews"
  - next_actions: "Pick a study resource", "Schedule first mock interview"

Initial Tasks created from committed next actions:

- Task: "Pick a system design study resource" (project_id → the project above, status: todo)

## Weekly Review

`weekly-review` surfaces:

- the goal has an active project, so it has momentum — no flag
- the task "Pick a system design study resource" is unblocked and not yet started — suggested as next action
